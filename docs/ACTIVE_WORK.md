# Active Work

Небольшой управляемый список активных задач — не backlog. Конкретные задачи и промпты для второго разработчика назначает GPT-координатор после завершения подготовки репозитория к параллельной работе.

| Задача | Исполнитель | Ветка | Разрешённые области | Запрещённые области | Статус |
|---|---|---|---|---|---|
| v7.1.17.1 Schedule Data Quality & Light Theme | Primary developer / primary Claude | текущая ветка основной разработки | `schedule_domain.py`; `schedule_sync.py`; schedule-секция `storage.py`; schedule routes в `web_app_server.py`; schedule UI в `miniapp/app.js`; schedule CSS в `miniapp/styles.css`; schedule tests | перечисленные schedule-области — для второго разработчика, до завершения integration | in progress |
| _(следующая задача второго разработчика)_ | | | | | |

## Правило приоритета

Перед началом работы разработчик обязан прочитать этот файл и свой точный task prompt. При расхождении между этим файлом и task prompt — task prompt имеет приоритет, но только в пределах явно разрешённых в нём файлов/областей.

Schedule-области, закреплённые за v7.1.17.1, не изменяются вторым разработчиком до завершения integration.

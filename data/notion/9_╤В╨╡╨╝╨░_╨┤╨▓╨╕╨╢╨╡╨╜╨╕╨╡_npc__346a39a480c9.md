# 9 тема - Движение NPC

Источник: Notion
Notion page ID: 346a39a4-80c9-8008-957a-c789302c7e98
Notion URL: https://app.notion.com/p/9-NPC-346a39a480c98008957ac789302c7e98
Notion last edited: 2026-05-10T06:52:00.000Z
Путь Notion: 30ca39a4 / Продукт / Программа обучения / Темы / 9 тема - Движение NPC

<callout icon="🎯" color="gray_bg">
	**Цель**
	<empty-block/>
</callout>
<callout icon="🔨" color="gray_bg">
	**Задачи**
	<empty-block/>
</callout>
<callout icon="✅" color="gray_bg">
	**Ожидаемый результат**
	<empty-block/>
</callout>
<callout icon="📂" color="gray_bg">
	**Материалы**
	<empty-block/>
</callout>
<callout icon="📃" color="gray_bg">
	**План**
	<table_of_contents color="gray"/>
</callout>
```lua
local Players = game:GetService("Players")
local PathfindingService = game:GetService("PathfindingService")

local npc = script.Parent
local humanoid = npc:WaitForChild("Humanoid")
local root = npc:WaitForChild("HumanoidRootPart")

local FollowDistance = 5
local UpdateTime = 0.01

local run = npc:WaitForChild("RunAnim")

local animator = humanoid:FindFirstChildOfClass("Animator")
if not animator then
	animator = Instance.new("Animator")
	animator.Parent = humanoid
end

local RunTrack = animator:LoadAnimation(run)
RunTrack.Priority = Enum.AnimationPriority.Movement
RunTrack.Looped = true

local function getClosestPlayer()
	local closestPlayer = nil
	local closestDistance = math.huge

	for _, player in pairs(Players:GetPlayers()) do
		if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
			local dist = (root.Position - player.Character.HumanoidRootPart.Position).Magnitude
			if dist < closestDistance then
				closestDistance = dist
				closestPlayer = player
			end
		end
	end

	return closestPlayer, closestDistance
end


while true do
	local player, distance = getClosestPlayer()

	if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
		local target = player.Character.HumanoidRootPart.Position

		if distance > FollowDistance then
			if not RunTrack.IsPlaying then
				RunTrack:Play(0.15)
			end

			local path = PathfindingService:CreatePath({
				AgentRadius = 2,
				AgentHeight = 5,
				AgentCanJump = true
			})

			path:ComputeAsync(root.Position, target)

			if path.Status == Enum.PathStatus.Success then
				local waypoints = path:GetWaypoints()

				if waypoints[2] then
					humanoid:MoveTo(waypoints[2].Position)
				else
					humanoid:MoveTo(target)
				end
			end
		else

			if RunTrack.IsPlaying then
				RunTrack:Stop(0.15)
			end
			humanoid:MoveTo(root.Position)
		end
	end

	task.wait(UpdateTime)
end
```
```lua
-- Получаем сервисы Roblox (готовые системы)
local Players = game:GetService("???") -- где хранятся все игроки
-- Players - хранит всех игроков в игре
local PathfindingService = game:GetService("???") -- строит путь для NPC
-- PathfindingService - умеет строить путь (обходить стены)
-- не узнает, за кем идти
-- не сможет обходить препятствия

-- Сам NPC
local npc = script.Parent

-- Главные части NPC
local humanoid = npc:WaitForChild("???") -- управляет движением
-- Humanoid = “мозг движения” NPC
local root = npc:WaitForChild("???") -- точка, откуда считаем позицию
-- HumanoidRootPart = его позиция в мире
-- Мы считаем расстояние именно от root, потому что
-- это центр персонажа
-- он всегда есть

-- Настройки
local FollowDistance = ??? -- на каком расстоянии остановиться
-- 5 — чтобы NPC не врезался в игрока
local UpdateTime = ??? -- как часто обновлять поведение
-- 0.1 — чтобы не нагружать игру (0.01 слишком часто)

-- Анимация
local run = npc:WaitForChild("???") -- анимация бега
-- RunAnim

-- Animator (проигрывает анимации)
local animator = humanoid:FindFirstChildOfClass("???") -- -- Animator - специальный объект для анимаций

if not animator then
	animator = Instance.new("???") -- Animator - специальный объект для анимаций
	animator.Parent = humanoid
end

local RunTrack = animator:LoadAnimation(run)

RunTrack.Priority = Enum.AnimationPriority.??? -- приоритет -- Movement — говорит: “это анимация движения”
RunTrack.Looped = ??? -- повторяется ли анимация -- true

-- Поиск ближайшего игрока
local function getClosestPlayer()
	local closestPlayer = nil
	local closestDistance = math.??? -- очень большое число

	for _, player in pairs(Players:GetPlayers()) do
		
		if player.Character and player.Character:FindFirstChild("???") then
			
			local dist = (root.Position - player.Character.HumanoidRootPart.Position).Magnitude
			
			if dist < closestDistance then
				closestDistance = dist
				closestPlayer = player
			end
		end
	end

	return closestPlayer, closestDistance
end

-- Главный цикл
while true do
	
	local player, distance = getClosestPlayer()

	if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
		
		local target = player.Character.HumanoidRootPart.Position

		if distance > FollowDistance then
			
			if not RunTrack.IsPlaying then
				RunTrack:Play(???)
			end

			local path = PathfindingService:CreatePath({
				AgentRadius = ???,
				AgentHeight = ???,
				AgentCanJump = ???
			})

			path:ComputeAsync(root.Position, target)

			if path.Status == Enum.PathStatus.??? then
				
				local waypoints = path:GetWaypoints()

				if waypoints[2] then
					humanoid:MoveTo(waypoints[2].Position)
				else
					humanoid:MoveTo(target)
				end
			end
		
		else
			
			if RunTrack.IsPlaying then
				RunTrack:Stop(???)
			end

			humanoid:MoveTo(???)
		end
	end

	task.wait(UpdateTime)
end
```
<callout icon="📝">
	**Компоненты для оформления**
	<callout icon="🧑‍🎓" color="gray_bg">
		Вопрос преподавателя
		<callout icon="🙋‍♂️" color="yellow_bg">
			Прогнозируемый ответ учеников.
		</callout>
	</callout>
	> *Пример*
	<callout icon="💡" color="gray_bg">
		Важное примечание
	</callout>
	<callout icon="☑️" color="gray_bg">
		Задание
	</callout>
	<callout icon="📂" color="gray_bg">
		Материалы
	</callout>
</callout>
